const { getEmptyBoard } = require("./utils");

const player1 = {
    counter: 0,
    ownBoard: getEmptyBoard(),
    rivalBoard: getEmptyBoard(), 
}

const player2 = {
    counter: 0,
    ownBoard: getEmptyBoard(),
    rivalBoard: getEmptyBoard(), 
}
